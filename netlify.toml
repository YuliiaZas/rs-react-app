[headers.values]
  HEADER_PLACEHOLDER = "api"

[[redirects]]
  from = "/api/*"
  to = "HEADER_PLACEHOLDER/:splat"
  force = true

[build]
command = "sed -i \"s|HEADER_PLACEHOLDER|${API_URL}|g\" netlify.toml && yarn build"
